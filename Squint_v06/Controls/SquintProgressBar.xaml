<UserControl x:Class="SquintScript.Controls.SquintProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SquintScript.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ProgressBar Height="25" BorderBrush="Black" Minimum="0" Maximum="100" Margin="10,0,5,0" VerticalAlignment="Stretch"  Template="{StaticResource ProgressBarNC}" IsIndeterminate="True"/>
        <TextBlock Text="{Binding LoadingString, UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
