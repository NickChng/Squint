<UserControl x:Class="Squint.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Squint.Views"
             mc:Ignorable="d" 
             xmlns:Views ="clr-namespace:Squint.Views"
             xmlns:ViewModels="clr-namespace:Squint.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=ViewModels:StartupViewModel, IsDesignTimeCreatable=True}">
    <UserControl.DataContext>
        <ViewModels:StartupViewModel/>
    </UserControl.DataContext>
    <Grid Background="White" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="{Binding InitializingMessages, UpdateSourceTrigger=PropertyChanged}" FontSize="18" FontWeight="Bold"/>
        <Views:SquintProgressBar Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Height="50" Width="500" />
    </Grid>

</UserControl>
